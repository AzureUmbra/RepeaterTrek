name: Semgrep
on:
  push:
    branches: [master]
jobs:
  semgrep:
    name: semgrep
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Semgrep
      id: semgrep
      uses: returntocorp/semgrep-action@v1
      with:
        publishToken: ${{ secrets.SEMGREP_APP_TOKEN }}
        publishDeployment: 44
#        config: r/all
